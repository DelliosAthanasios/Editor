FROM node:20-alpine

RUN apk add --no-cache \
    git \
    build-essential \
    python3 \
    curl

RUN npm install -g \
    typescript \
    ts-node \
    yarn \
    pnpm

WORKDIR /workspace
EXPOSE 3000 5000 8080

ENV NODE_ENV=development

CMD ["/bin/sh"]
